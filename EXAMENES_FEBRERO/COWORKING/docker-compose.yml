services:
  server-php-coworking:
    container_name: servidor_php_coworking
    build:
      dockerfile: ./Dockerfile
      context: .
    restart: unless-stopped
    stdin_open: true
    links:
      - mariadb-server-coworking
    ports:
      - "8030:8000"
    volumes:
      - ../:/var/www/html:delegated
    networks:
      - project_network

  mariadb-server-coworking:
    container_name: servidor_db_coworking
    image: mariadb
    ports:
      - "3330:3306"
    restart: unless-stopped
    environment:
      MYSQL_USER: root
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - db_mysql:/var/lib/mysql
    networks:
      - project_network

volumes:
  db_mysql:

networks:
  project_network: